spring:
  application:
    name: decisioniq
  config:
    import: classpath:decisioniq-data-slices.yaml
openai:
  base-url: https://api.openai.com/v1
  api-key: ${OPENAI_API_KEY}
  model: ${MODEL_NAME}
